# Firebase CLI local cache/logs — not part of the source, regenerated on deploy.
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log

# Claude Code session artifacts — not part of the submission.
.claude/

# Editor
.vscode/
.idea/
*.iml

# OS junk
.DS_Store
Thumbs.db

# Never commit real secrets if one ever ends up here.
.env
*.env.local
serviceAccountKey.json
